{% extends 'base.html.twig' %}

{% block body %}
   <a class="btn btn-danger btn-sm" href="{{ path('user_index') }}">Back to the list</a>
   
    <h1>Ajouter un ami</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

         
{% endblock %}
