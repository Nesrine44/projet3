{% extends 'base.html.twig' %}
{% block body %}
   <a href="{{ path('user_new') }}" class="btn btn-danger btn-sm">Create a new entry</a>
<hr>
    <h2>Liste @'adresse</h2>
<table class="table table-bordered  table-hover"> 

        <thead>
            <tr>

            		 <th>id</th>
		 <th>Nom</th>
          <th>Email</th>
          
                         
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td> <a href="{{ path('user_show', { 'id': user.id }) }}">{{ user.id }}</a></td>
                
                <td>{{ user.username }}</td>
       
                 <td>{{ user.email }}</td>
            
                <td>
                  
                            <a class="btn btn-success btn-sm" href="{{ path('user_show', { 'id': user.id }) }}" >show</a>
                      <a class="btn btn-default btn-sm" href="{{ path('user_delete', { 'id': user.id }) }}">delete</a>
                            <a class="btn btn-danger btn-sm" href="{{ path('user_edit', { 'id': user.id }) }}"  >edit</a>
                  
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

         

{% endblock %}
